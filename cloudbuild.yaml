steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/yoga-chatbot', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/yoga-chatbot']

  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'yoga-chatbot'
      - '--image=gcr.io/$PROJECT_ID/yoga-chatbot'
      - '--region=us-central1'
      - '--allow-unauthenticated'
      - '--port=8080'
